---
interface Props {
  title: string;
}

const { title } = Astro.props;
const { pathname } = Astro.url;

const listItemClassName = "flex justify-center flex-col";
const paths = [
  {
    address: "/",
    title: "Home",
    class: listItemClassName,
  },
  {
    address: "/about",
    title: "About",
    class: listItemClassName,
  },
  {
    address: "/xperience",
    title: "Xperience",
    class: listItemClassName,
  },
  {
    address: "/contact",
    title: "Contact",
    class: listItemClassName,
  },
  {
    address: "/blog",
    title: "Blog",
    class: listItemClassName,
  },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="p-4 font-helvetica bg-gradient-to-l from-emerald-500  to-blue-700  flex flex-col items-center">
    <header class="w-3/4 h-full">
      <nav
        class="w-full h-12  flex justify-center"
      >
        <ul class="flex flex-col sm:flex-row justify-evenly  items-end  w-1/2 h-full ">
          {
            paths.map((path) => {
              return (
                <li class=`${path.class} ${pathname === path.address ? "active" : ""} `>
                  
                  <a href=`${path.address}`>
                  {pathname === path.address ? "🌏 " : ""}
                  {path.title} 
                  </a>
                  </li>
              );
            })
          }
        </ul>
      </nav>
    </header>
    <main class="w-3/4  mt-10 bg-gradient-to-r from-blue-900">
      <slot />
    </main>
  </body>
</html>
<style is:global>
  li {
    color: rgb(255, 255, 255);
    display: inline-block;
    font-size: 1.3rem;
    height: 25px;
    padding: 15px;
    text-align: center;
  }
 
  li.active {
    background-color: rgba(151, 188, 243, 0.698);
    border-radius: 12px;
  }

  main {
    height: 85vh;
    border-radius: 20% 12px 20% 12px;
  }
 </style>
